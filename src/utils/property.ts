// import type { AlkopolyPlayer } from "../types/PlayerProps";

import type { TileProps } from "../types/TileProps";

export const buyProperty = (tile: TileProps /* player: AlkopolyPlayer */) => {
  console.log(tile.name);
};
